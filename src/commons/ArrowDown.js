import React, {Component} from 'react';
import Icon from './Icon';

class ArrowDown extends Component {
    render() {
        return (
            <Icon name="feat|arrow-down"
                  size={18}
                  color={'red'}
            />
        );
    }
}

export default ArrowDown;